const API="https://hinova-site.hinovadigital.workers.dev";
document.addEventListener("DOMContentLoaded",()=>{const b=document.getElementById("navBurgerBtn"),m=document.getElementById("navDropdown");if(b&&m){b.onclick=e=>{e.stopPropagation();let o=m.classList.toggle("open");b.setAttribute("aria-expanded",o)};document.addEventListener("click",e=>{if(!m.contains(e.target)&&e.target!==b)m.classList.remove("open")})}const top=document.getElementById("backToTop");if(top){addEventListener("scroll",()=>top.classList.toggle("visible",scrollY>400),{passive:true});top.onclick=()=>scrollTo({top:0,behavior:"smooth"})}const ob=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add("visible")),{threshold:.1});document.querySelectorAll(".reveal").forEach(x=>ob.observe(x));if("serviceWorker"in navigator)navigator.serviceWorker.register("/sw.js").catch(()=>{});if(document.getElementById("ecosystem-grid"))load("/ecosystem/","ecosystem-grid");if(document.getElementById("solutions-grid"))load("/offres","solutions-grid");if(document.getElementById("questions-container"))loadQuestions()});
async function get(p){let r=await fetch(API+p);if(!r.ok)throw Error(r.status);return r.json()}function esc(v=""){return String(v).replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]))}
async function load(endpoint,id){const g=document.getElementById(id);try{const d=await get(endpoint),rs=(d.records||[]).filter(r=>r.fields&&r.fields.Nom);if(!rs.length)return;g.innerHTML=rs.map((r,i)=>{let f=r.fields;let href=endpoint==="/offres"?"/parcours/":i===0?"/solutions/":i===1?"/academie.html":"/parcours/";return `<article class="glass p-10 card-hover reveal"><p class="gold-label">${esc(f.Type||"SOLUTION")}</p><h3 class="font-montserrat font-bold text-xl mt-4" style="color:#0AB8C4">${esc(f.Titre||f.Nom)}</h3><p class="solution-price mt-5">${endpoint==="/offres"&&f["Prix création XPF"]?Number(f["Prix création XPF"]).toLocaleString("fr-FR")+" F CFP":""}</p><p class="font-inter text-sm leading-relaxed mt-3" style="color:#81A1A8">${esc(f["Sous-titre"]||f.Notes||"Solution HINOVA DIGITAL.")}</p><a href="${href}" class="btn-cyan mt-6">${endpoint==="/offres"?"IDENTIFIER MA TRAJECTOIRE":"DÉCOUVRIR"}</a></article>`}).join("");g.querySelectorAll(".reveal").forEach(x=>x.classList.add("visible"))}catch(e){}}
async function loadQuestions(){const box=document.getElementById("questions-container");try{const d=await get("/questions"),rs=(d.records||[]).filter(r=>r.fields&&r.fields.Question).sort((a,b)=>(a.fields.Ordre||0)-(b.fields.Ordre||0));let i=0;function render(){let f=rs[i].fields,o=f.Options;if(typeof o==="string"){try{o=JSON.parse(o)}catch{o=o.split(",").map(x=>x.trim()).filter(Boolean)}}if(!Array.isArray(o))o=[];box.innerHTML=`<p class="gold-label">QUESTION ${i+1} / ${rs.length}</p><h3 class="font-montserrat font-bold text-xl mt-3" style="color:#E0F7FA">${esc(f.Question)}</h3><div class="grid gap-3 mt-6">${o.map(x=>`<button class="question-option">${esc(x)}</button>`).join("")}</div>`;box.querySelectorAll("button").forEach(b=>b.onclick=()=>{i++;i<rs.length?render():finish()})}function finish(){document.getElementById("recommendation").innerHTML='<p class="gold-label">TRAJECTOIRE</p><h3 class="font-montserrat font-black text-2xl mt-4" style="color:#E0F7FA">Analyse terminée.</h3><p class="font-inter mt-3" style="color:#81A1A8">Les règles de trajectoire vont maintenant déterminer l’offre adaptée.</p><a href="/solutions/" class="btn-cyan mt-6">VOIR LES SOLUTIONS</a>'}rs.length?render():box.innerHTML="";}catch(e){box.innerHTML='<p style="color:#81A1A8">Le moteur de parcours est momentanément indisponible.</p>'}}
